services:
  staff-db:
    image: postgres:16
    container_name: staff-db
    ports:
      - "5433:5432"
    env_file:
      - .env
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data


volumes:
  postgres_data: